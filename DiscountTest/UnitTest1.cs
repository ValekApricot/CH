using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Collections.ObjectModel;

namespace DiscountTest
{
    [TestClass]
    public class UnitTest1
    {
        //[TestMethod]
        //    public void Test1MethodTHU150()
        //    {
        //        DateTime time = new DateTime(2023, 5, 11);
        //        double cost = 150;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> UnlessCollection = CoffeeHouse.ClassHelper.CartClass.stuffsCart;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> outp = CoffeeHouse.Windows.ClientWindows.CartWindow.DiscountTHU(time, cost.ToString(), UnlessCollection);

        //        Assert.AreEqual(UnlessCollection, outp);
        //    }

        //    [TestMethod]
        //    public void Test2MethodMON100()
        //    {
        //        DateTime time = new DateTime(2023, 5, 8);
        //        double cost = 100;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> UnlessCollection = CoffeeHouse.ClassHelper.CartClass.stuffsCart;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> outp = CoffeeHouse.Windows.ClientWindows.CartWindow.DiscountTHU(time, cost.ToString(), UnlessCollection);

        //        Assert.AreEqual(UnlessCollection, outp);
        //    }

        //    [TestMethod]
        //    public void Test3MethodFRISub100()
        //    {
        //        DateTime time = new DateTime(2023, 5, 11);
        //        double cost = -100;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> UnlessCollection = CoffeeHouse.ClassHelper.CartClass.stuffsCart;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> outp = CoffeeHouse.Windows.ClientWindows.CartWindow.DiscountTHU(time, cost.ToString(), UnlessCollection);

        //        Assert.AreEqual(UnlessCollection, outp);
        //    }

        //    [TestMethod]
        //    public void Test4MethodTHU0()
        //    {
        //        DateTime time = new DateTime(2023, 5, 11);
        //        double cost = 0;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> UnlessCollection = CoffeeHouse.ClassHelper.CartClass.stuffsCart;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> outp = CoffeeHouse.Windows.ClientWindows.CartWindow.DiscountTHU(time, cost.ToString(), UnlessCollection);

        //        Assert.AreEqual(UnlessCollection, outp);
        //    }

        //    [TestMethod]
        //    public void Test5MethodTHUIntMax()
        //    {
        //        DateTime time = new DateTime(2023, 5, 11);
        //        double cost = int.MaxValue;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> UnlessCollection = CoffeeHouse.ClassHelper.CartClass.stuffsCart;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> outp = CoffeeHouse.Windows.ClientWindows.CartWindow.DiscountTHU(time, cost.ToString(), UnlessCollection);

        //        Assert.AreEqual(UnlessCollection, outp);
        //    }

        //    [TestMethod]
        //    public void Test6MethodTHUDoubleMax()
        //    {
        //        DateTime time = new DateTime(2023, 5, 11);
        //        double cost = double.MaxValue;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> UnlessCollection = CoffeeHouse.ClassHelper.CartClass.stuffsCart;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> outp = CoffeeHouse.Windows.ClientWindows.CartWindow.DiscountTHU(time, cost.ToString(), UnlessCollection);

        //        Assert.AreEqual(UnlessCollection, outp);
        //    }

        //    [TestMethod]
        //    public void Test7Methodyear1150()
        //    {
        //        DateTime time = new DateTime(1, 5, 11);
        //        double cost = 150;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> UnlessCollection = CoffeeHouse.ClassHelper.CartClass.stuffsCart;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> outp = CoffeeHouse.Windows.ClientWindows.CartWindow.DiscountTHU(time, cost.ToString(), UnlessCollection);

        //        Assert.AreEqual(UnlessCollection, outp);
        //    }

        //    [TestMethod]
        //    public void Test8Methodyear1001150()
        //    {
        //        DateTime time = new DateTime(1001, 5, 11);
        //        double cost = 0;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> UnlessCollection = CoffeeHouse.ClassHelper.CartClass.stuffsCart;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> outp = CoffeeHouse.Windows.ClientWindows.CartWindow.DiscountTHU(time, cost.ToString(), UnlessCollection);

        //        Assert.AreEqual(UnlessCollection, outp);
        //    }

        //    [TestMethod]
        //    public void Test9Methodyear00010()
        //    {
        //        DateTime time = new DateTime(0001, 12, 11);
        //        double cost = 0;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> UnlessCollection = CoffeeHouse.ClassHelper.CartClass.stuffsCart;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> outp = CoffeeHouse.Windows.ClientWindows.CartWindow.DiscountTHU(time, cost.ToString(), UnlessCollection);

        //        Assert.AreEqual(UnlessCollection, outp);
        //    }

        //    [TestMethod]
        //    public void Test10MethodTHUA()
        //    {
        //        DateTime time = new DateTime(2023, 5, 11);
        //        string cost = "1";

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> UnlessCollection = CoffeeHouse.ClassHelper.CartClass.stuffsCart;

        //        ObservableCollection<CoffeeHouse.DataBase.Stuff> outp = CoffeeHouse.Windows.ClientWindows.CartWindow.DiscountTHU(time, cost, UnlessCollection);

        //        Assert.AreEqual(UnlessCollection, outp);
        //    }
    }
}
